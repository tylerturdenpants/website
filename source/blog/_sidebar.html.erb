<ol id='toc-list'>
  <% if blog.tags['Recent Posts'] %>
  <li class='level-1'>
    <a href="<%= tag_path 'Recent posts'%>"><span class="glyphicon glyphicon-chevron-right
" aria-hidden="true"></span> Recent Posts</a>
    <ol style="display:block">
      <% blog.tags['Recent Posts'].sort_by(&:date).reverse.take(5).each_with_index do |article, i| %>
      <li class='level-3'><%= link_to article.title, article %></li>
    <% end %>
    </ol>
  </li>
  <% end %>
  <li class='level-1'>
    <a href="<%= tag_path 'Releases'%>"><span class="glyphicon glyphicon-chevron-right
" aria-hidden="true"></span> Releases</a>
    <ol style="display:block">
      <% blog.tags['Releases'].sort_by(&:date).reverse.take(7).each_with_index do |article, i| %>
      <li class='level-3'><%= link_to article.title, article %></li>
    <% end %>
    </ol>
  </li>
</ol>
